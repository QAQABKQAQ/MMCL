import { Play } from "lucide-react";

export default function StartGame() {
  return (
    <button className="w-48 h-16 bg-blue-500 dark:bg-blue-500 text-white hover:bg-blue-500 dark:hover:bg-blue-400 rounded-l-full px-4 flex items-center justify-center gap-2">
      <span>启动游戏</span>
    </button>
  )
}